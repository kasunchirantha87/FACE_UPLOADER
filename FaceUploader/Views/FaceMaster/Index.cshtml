@model IEnumerable<FaceUploader.Models.FaceMaster>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.P0)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.P45)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.P90)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.P0)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.P45)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.P90)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
